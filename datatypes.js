// Data Types

// Primitive Data Types

// Strings
const name = "Jayti";
const language = "JavaScript";
const salary = `12,50,000`;

// Number -> integer and floating point values.
const num = 0;
const PI = 3.14;

// Boolean -> true/false.

const isNewUser = false;
const isPrimaryNumber = true;

// Undefined

let res;
console.log(res);

// Null -> empty/unknown value in JS.

const data = null;

// BigInt -> stores integer greater than number type can hold.

// Symbol -> stores unique and unchangeable value.

// Non-primitive data types

// Objects -> stores collection of data.

// Object literal -> stored as key-value pair.

const person = {
  name: "Jayti Srivastava",
  age: "25",
  employmentStatus: true,
  salary: " 12,50,000",
};
console.log(person.name);

// Arrays
const oddNum = [1, 3, 5, 7, 9];
console.log(oddNum[0]);
